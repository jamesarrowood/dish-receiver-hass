# Test tooling for the DISH receiver integration.
# Pin pytest-homeassistant-custom-component to a version matching the HA release
# you target; it pulls in homeassistant + pytest plugins transitively.
pytest>=8.0
pytest-asyncio>=0.23
pytest-homeassistant-custom-component>=0.13.0
